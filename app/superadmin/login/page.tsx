'use client';
import { useState } from 'react';
export default function SuperadminLoginPage() { const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [loading,setLoading]=useState(false); const [error,setError]=useState(''); async function onSubmit(e: React.FormEvent){e.preventDefault();setLoading(true);setError(''); const res=await fetch('/api/superadmin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})}); setLoading(false); if(!res.ok){const body=await res.json().catch(()=>({})); setError(body?.error??'Sikertelen bejelentkezés'); return;} window.location.href='/superadmin';}
return <main className="app-surface flex min-h-screen items-center justify-center bg-[radial-gradient(ellipse_at_top,rgba(20,184,166,0.06)_0%,transparent_55%)] px-4"><form onSubmit={onSubmit} className="w-full max-w-md rounded-2xl border border-white/[0.08] bg-white/[0.04] p-7 shadow-panel"><h1 className="text-2xl font-semibold text-slate-100">Platform Vezérlőpult — Superadmin</h1><p className="mt-2 text-sm text-slate-400">Belépés a manuális integrációs indításokhoz és monitorozáshoz.</p><div className="mt-5 space-y-3"><input className="input-base" type="email" placeholder="Email" value={email} onChange={(e)=>setEmail(e.target.value)} required /><input className="input-base" type="password" placeholder="Jelszó" value={password} onChange={(e)=>setPassword(e.target.value)} required /></div>{error ? <p className="mt-3 rounded-xl bg-rose-500/10 px-3 py-2 text-sm text-rose-300 ring-1 ring-rose-500/25">{error}</p> : null}<button className="btn-primary mt-5 w-full py-3" disabled={loading}>{loading ? 'Beléptetés...' : 'Belépés'}</button></form></main>; }
