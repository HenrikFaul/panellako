'use client';
import { useState } from 'react';
export default function SuperadminLoginPage() { const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [loading,setLoading]=useState(false); const [error,setError]=useState(''); async function onSubmit(e: React.FormEvent){e.preventDefault();setLoading(true);setError(''); const res=await fetch('/api/superadmin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})}); setLoading(false); if(!res.ok){const body=await res.json().catch(()=>({})); setError(body?.error??'Sikertelen bejelentkezés'); return;} window.location.href='/superadmin';}
return <main className="app-surface flex min-h-screen items-center justify-center px-4 py-8" style={{ backgroundImage: 'none' }}><form onSubmit={onSubmit} className="w-full max-w-md rounded-[1.25rem] border border-canvas-line bg-white p-7 shadow-card-md"><h1 className="text-2xl font-semibold text-canvas-ink">Platform Vezérlőpult — Superadmin</h1><p className="mt-2 text-sm text-canvas-muted">Belépés a manuális integrációs indításokhoz és monitorozáshoz.</p><div className="mt-5 space-y-3"><input className="input-base" type="email" placeholder="Email" value={email} onChange={(e)=>setEmail(e.target.value)} required /><input className="input-base" type="password" placeholder="Jelszó" value={password} onChange={(e)=>setPassword(e.target.value)} required /></div>{error ? <p className="mt-3 rounded-xl border border-rose-200 bg-rose-50 px-3 py-2 text-sm text-rose-800">{error}</p> : null}<button className="btn-primary mt-5 w-full py-3" disabled={loading}>{loading ? 'Beléptetés...' : 'Belépés'}</button></form></main>; }
