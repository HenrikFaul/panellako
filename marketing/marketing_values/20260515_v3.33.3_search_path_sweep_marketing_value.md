# v3.33.3 — Public-function search_path sweep · Marketing value record

**Engineering record:** `versioning/15052602_v3.33.3_public_function_search_path_sweep.md`

## User-language problem solved

"The Supabase security advisor's `function_search_path_mutable`
warnings on `public` schema are at zero. Future regressions are
caught by a vitest test that runs on every commit."

Pure infra/security hygiene release — not user-visible.

## Primary personas

- **Security reviewer** — wants the security advisor green on the
  schema where customer data lives.
- **SOC 2 auditor** — wants documented evidence that every
  SECURITY DEFINER / trigger function declares an explicit
  search_path.
- **Platform engineer** — wants the "advisor warnings" backlog to
  trend to zero rather than grow.

## Marketing-ready claim

"Effectime treats security-advisor lints as bugs, not noise: when a
warning appears on a function in our customer-data schema, we
audit, patch, and lock it in with a regression test in the same
release."

## Claim types enabled
- **Continuous hardening** — we don't wait for an external audit
  to clean up known patterns.
- **Test-enforced conventions** — every protected function is
  asserted at PR time, not just at advisor-scan time.

## Funnel stage
**Bottom funnel** — security questionnaire and SOC 2 evidence
packet support. Cite alongside v3.33.2 as a paired "audit cycle
artifact": v3.33.2 fixed the new regressions, v3.33.3 cleared the
pre-existing backlog.

## Marketing files to update
- `marketing/positioning/trust_and_reliability.md` — append:
  "Security advisor warnings on `public` schema functions: zero."

## What does NOT change
- No user-visible feature.
- No new tier, plan, pricing, or copy.
