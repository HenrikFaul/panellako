# v3.33.2 — Tier-change RPC marker + search_path hotfix · Marketing value record

**Engineering record:** `versioning/15052601_v3.33.2_tier_marker_and_search_path_hotfix.md`

## User-language problem solved

"Tier upgrades that the platform team approves actually go through the
audit-tracked path. The database-level guard we added doesn't
accidentally lock everyone out, and the security audit's
search-path-injection finding is closed."

Pure infra/security release — not user-visible.

## Primary personas

- **Platform engineer** — wants to know that adding a strict DB
  invariant didn't break the legitimate path that uses it.
- **Security reviewer** — wants Supabase advisor warnings on touched
  functions to be at zero, not "we'll fix it later."
- **Compliance lead** — needs the tier-change audit row to actually
  be written when a tier change happens.

## Marketing-ready claim

"Effectime ships database-level governance with the same care as
application code: when we add an enforcement trigger, we verify the
RPC that's expected to bypass it actually does, before any customer
operation hits the new code path."

## Claim types enabled
- **Engineering rigor** — we read live function bodies, not just
  documentation, before shipping a guard that depends on them.
- **Security posture** — every SECURITY DEFINER function we touch
  declares `SET search_path` (closes Supabase lint
  `function_search_path_mutable` on our changes).
- **Regression net** — a 21-test suite scans the migrations corpus
  on every commit; the same class of bug cannot recur silently.

## Content angles
- "Why we ran the same audit twice — and what we caught the second
  time" — engineering blog post on continuous self-audit.
- "Migration-invariant tests: turning conventions into compile-time
  guarantees" — short technical note.

## Funnel stage
**Bottom funnel** — trust signals only. Cite in security questionnaires
("we ship advisor-clean migrations") and SOC 2 evidence packets.

## Marketing files to update
- `marketing/positioning/trust_and_reliability.md` — append:
  "Migration invariants enforced via vitest regression tests."
- `marketing/comparison_tables/audit_capabilities.md` — append:
  "DB advisor warnings on shipped functions" → ✓ zero.

## What does NOT change
- No user-visible feature.
- No new tier, plan, pricing, or copy.
