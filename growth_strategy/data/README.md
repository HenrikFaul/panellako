# data/

This folder is filled in by the AI agent (see `../AI_INSTRUCTIONS.md`).

Expected files after the AI runs:
- `project.json`            — branding, languages, valuation figures
- `growth_strategy.json`    — ranked growth initiatives
- `valuation/en.json`, `valuation/hu.json`, …  — per-language valuation content
- `repo_scan.json`          — produced automatically by `../scan_repo.py`

To preview the toolkit immediately, copy the files from `../examples/` here.
