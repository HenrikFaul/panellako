# v0.5.6 — UI/UX Layout Refactor: Overflow fixes, responsive grids, feed health

**Date:** 2026-05-17  
**Branch:** `claude/setup-project-structure-O56pg`  
**Type:** Enhancement / Bug fix

## Summary

Systematic layout audit and fix pass based on UI_UX_REFACTOR_MASTER_PROMPT.md. Eliminates horizontal scroll on all breakpoints, adds missing responsive grid steps, and contains animated SVG elements within their panels.

## Changes

### Global overflow containment (Phase 8)
- `app/globals.css`: `overflow-x: hidden` + `max-width: 100%` on `html` and `body`
- `app/layout.tsx`: `overflow-x-hidden` Tailwind class on `<body>`

### Dashboard layout fixes (Phase 2, 3, 5, 9)
- `components/dashboard-client.tsx`:
  - Overview right panel visibility: `hidden md:flex` → `hidden lg:flex`
  - Metric cards grid: added `lg:grid-cols-3` intermediate step between `sm:grid-cols-2` and `xl:grid-cols-4`
  - Header search input: `w-56` → `w-full md:w-56` (full width on mobile)
  - Building `<h1>`: added `break-words` (long addresses no longer push layout)

### Component internals (Phase 7)
- `components/transport-panel.tsx`: Tab row wraps with `flex-wrap` on narrow panels
- `components/energy-dashboard.tsx`: `MeterCard` wrapper has `overflow-hidden` (donut gauge contained)
- `components/air-quality-widget.tsx`: AQIcon container has `overflow-hidden` (particle animations clipped)

## Testing

- TypeScript: `npx tsc --noEmit` — 0 errors
- Verified no regressions to existing features

## Governed by

- `UI_UX_REFACTOR_MASTER_PROMPT.md` — Phases 2, 3, 5, 7, 8, 9
- `.governance/ui_ux_rules.md`
