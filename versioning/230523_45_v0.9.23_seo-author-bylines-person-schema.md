# v0.9.23 — SEO Sprint 2: Author bylines, CollectionPage, Person schema, stats citation

**Dátum:** 2026-05-23
**Branch:** claude/fix-cron-null-values-uDCqd
**Backlog items:** ACT-024, ACT-025, ACT-026, ACT-059

## Changes

### ACT-024: Visual author bylines on 28 article pages
- Injection via Python batch script: finds `</nav>` after `<main>` in each article page
- Byline pattern: `PanelLakó szerkesztőség · 2024. január 15. · Frissítve: 2026. május 23.`
- One edge case (budapest-kozlekedes — no nav): injected inside `<header>` above `</header>`
- One edge case (alapito-okirat): needed separate injection call (was missed in batch due to sort order issue)
- Files: 28 article pages across all content pillars

### ACT-059: CollectionPage schema on /tarsashaz-kezeles hub
- `collectionPageSchema` const added before `faqSchema`
- Schema has 8 `hasPart: [{ '@type': 'Article', ... }]` entries matching PILLAR_ARTICLES array
- Third `<script>` tag added in JSX

### ACT-026: Named team member + Person schema on /rolunk
- `const TEAM = [{ name: 'Faul Henrik', role: 'Alapító & Termékfejlesztés', bio: '...' }]`
- `const personSchemas` generates Person JSON-LD array
- Script tags: `{personSchemas.map(ps => <script key={ps.name} .../>)}`
- Generic team blob replaced with mapped card grid (name, role badge, bio)

### ACT-025: Trust statistics citation footnote
- `/rolunk/page.tsx`: `STATS` array expanded with `note` field; footnote list rendered below the 5-star row
- `/page.tsx`: hero stats strip gains a `<p>` footnote — "Saját üzemeltetési adatok, 2025. május" with link to /rolunk

## Risk assessment
- All changes are additive — no existing rendering logic removed
- TypeScript: no new errors introduced; all pre-existing typedRoutes errors unchanged
- Author byline dates are static (2024-01-15 / 2026-05-23) matching articleSchema fields set in v0.9.20
